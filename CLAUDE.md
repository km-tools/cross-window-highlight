# クロスウィンドウ・ハイライト（Chrome 拡張）

## 作業の進め方

1. **最初に `00_いまここ.md` を読む**。現在地・決定事項・環境メモが書いてある
2. 計画書 `cwh-release-plan.md` の Phase 順に進める。完了条件を満たしてから次へ
3. **1つの作業が終わるたびに `00_いまここ.md` を更新する**（現在地・作業ログ・決定事項）。報告より先に記録する
4. 判断に迷う点（名前、ライセンス API 仕様、iframe の副作用など）は勝手に決めず報告して止まる

## 構成

- `src/` 拡張本体（これをストアに出す）
- `tools/` アイコン生成・e2e テスト（npm 依存はここだけ）
- `製品/` 元データ。触らない

## 守ること

- 計画書「壊してはいけない挙動」は変更しない。追加は既存関数を呼ぶ側で行う
- 外部通信はライセンス検証の1本だけ。新しい permission は追加しない
- 変更後は `node tools/e2e.js --lang=ja` と `--lang=en` を通す
- コミットは Phase 単位＋機能単位で細かく
