# クロスウィンドウ・ハイライト いまここ

作業の記録。セッション開始時に必ずここを読んでから始め、1つの作業が終わるたびに更新する。
計画書は [cwh-release-plan.md](cwh-release-plan.md)。上から Phase 順に進める。

## 現在地

- **Phase 4 完了**（2026-09-10）。ストア説明文・スクショ 3 枚・permissions_justification・package.sh まで揃った
- 残りは **Polar sandbox の実機確認**（要: sandbox の Organization ID とテストキー）と、公開前の本番設定（`license.js` の organizationId / purchaseUrl）
- ストア提出前は `./package.sh --verify` を通してから `dist/` の ZIP を上げる

## 決定事項

- 英語名は **Cross-Window Highlight** で確定（ストアに同名なし、2026-09-07 確認）
- 拡張本体は `src/`。`製品/` の展開物は元データとして残すだけで触らない（.gitignore 済み）
- ビルドツールなし。素の JS。ツール類は `tools/`（npm 依存はここだけ）
- ON/OFF はポップアップのスイッチとショートカット。アイコンクリックでのトグルは廃止
- **販売プラットフォームは Polar.sh**（2026-09-07 決定）
  - 検証は customer-portal 系の validate / activate エンドポイント（認証不要・公開クライアント向け）
  - 席数はライセンスキーのアクティベーション上限で表現
  - 実装前に sandbox でテスト。API 仕様は docs.polar.sh を確認して README に記録
  - 問題が出た場合の切り替え先は Gumroad。`license.js` は差し替え可能な構造を維持
- **Phase 4 の説明文の方針**（2026-09-10 決定）
  - 「ミスをゼロに」「○%削減」などの断定を使わない
  - 「人による確認を支援する」「選択するだけで別画面の一致箇所を確認できる」の表現で書く
  - 正規化して一致した箇所は色が違うことを、説明文でも明記する

## 環境メモ

- Chrome 152 では `--load-extension` が効かない。自動テストは puppeteer-core の `installExtension` 経由
- 手動確認は `chrome://extensions` →「パッケージ化されていない拡張機能を読み込む」→ `src/` を選ぶ
- 自動テスト: `node tools/e2e.js --lang=ja` / `--lang=en`（実機 Chrome が開いて閉じる。22項目）
- Pro テスト: `node tools/e2e-pro.js`（モック Polar を内蔵。60項目）。A は localhost、B は 127.0.0.1 で配信して除外ドメインを片側に効かせている
- Polar の接続先上書き: `chrome.storage.local` の `cwh_dev = { apiBase, organizationId }`（README 参照）
- Polar API の出典は `https://polar.sh/docs/openapi.json`。docs.polar.sh の個別ページは 404 になる
- アイコン再生成: `node tools/build-icons.js`（元絵は `src/icons/icon.svg`）
- スクショ撮り直し: `node tools/shoot-store.js`（store/screenshots/ に 1280×800 を 3 枚。ダミーページは store/screenshots/pages/）
- ZIP: `./package.sh`（Python の zipfile で作る。Git Bash に zip が無いため）。`--verify` で ZIP からの e2e
- e2e 系は環境変数 `CWH_EXT_DIR` で読み込む拡張フォルダを差し替えられる

## 作業ログ

### 2026-09-07 Phase 1 完了

- `src/` へ移動、git init、機能単位で4コミット
- アイコン（SVG + 16/32/48/128 PNG）
- ポップアップ `src/popup/`（ON/OFF、ハイライト中文字列、ショートカット、Pro 欄は準備中）
- 多言語化 `src/_locales/{ja,en}`。manifest とトーストを `__MSG__` / `chrome.i18n` に
- バージョン 3.0.0、休止タブ（discarded）除外、`action.onClicked` 削除、`SET_ENABLED` メッセージ追加
- e2e 自動テスト ja/en 各 22/22 PASS。「壊してはいけない挙動」は全て確認済み

### 2026-09-07 Phase 2 完了

- `docs/privacy.md`（日英併記）: 収集データなし、選択文字列は端末内のみ、各 permission の理由、Pro のライセンス検証は「準備中」として先に記載
- `docs/index.md`: GitHub Pages 用トップ。GitHub / ストアのリンクは「準備中」のまま
- `README.md`（日本語）: インストール、使い方、ショートカット、無料/Pro 比較表、構成、開発コマンド
- 未決だったライセンス・リポジトリ URL は 2026-09-07 に確定（下記）

### 2026-09-07 Phase 3 実装（モック検証まで）

- `src/settings.js`: Pro 設定の既定値・sanitize・読み書き。無効時は `cwhEffectiveSettings` がデフォルトを返す
- `src/license.js`: Polar プロバイダ。activate → 403 なら validate で「上限なしキー」を判別。7日再検証、オフラインは前回結果、deactivate
- background: 除外ドメイン（`isBlocked` 拡張）、ヒット件数バッジ、iframe 注入、`LICENSE_*` メッセージ
- content: 色・最小文字数・除外・複数語（2語以上のときだけ `highlightMulti`）・iframe。サブフレームは応答を返さない（親の件数を守るため）
- popup: ライセンス入力／解除／再確認、設定フォーム（無効時は disabled）
- manifest: `all_frames: true`、content_scripts に `settings.js` を追加。permission は増やしていない
- `tools/e2e-pro.js` 38/38、Phase 1 回帰 22/22 ×2。通信はライセンス API の3本のみ、送信内容にキー以外なし
- 計画書との差分: 保存形式に生の `key` も含めた（7日再検証に必要。端末内のみ）
- 未設定: `CWH_LICENSE_CONFIG.organizationId` / `purchaseUrl`（Polar のアカウント作成はユーザー側で）

### 2026-09-07 GitHub 公開

- リポジトリ: https://github.com/km-tools/cross-window-highlight（Public、MIT。`LICENSE` 追加、著作権表示は km-tools）
- GitHub Pages: https://km-tools.github.io/cross-window-highlight/ の前提で README / docs のリンクを確定。`docs/_config.yml`（primer テーマ）を追加
- README の「準備中」で残っているのは Chrome ウェブストアと Pro の提供だけ
- Pages 有効化済み（2026-09-07）。トップ・`privacy.html`・`/privacy` すべて 200 を確認。ストア申請用のプライバシーポリシー URL は `https://km-tools.github.io/cross-window-highlight/privacy.html`
- トップページは primer テーマの素の Markdown。Phase 4 でストア説明文とスクショができたら、同じ素材で LP に作り直す（先に LP を作ると二度手間）

### 2026-09-10 Phase 3 補足（誠実な表示）

- **完全一致 / 正規化一致の色分け**: mark に `cwh-norm`（薄い橙 + 点線、title で説明）。input は破線枠。無料版でも色分けあり。色の変更は Pro
- **厳密比較モード**（Pro 設定 `strictMatch`）: 正規化せず生文字列の indexOf のみ。複数語とも併用可
- content.js の `highlightNode` は判定ロジック不変、mark 生成時に `decorateMark` を足しただけ。応答に `exact` / `norm` を追加
- **判定不能の区別**: background が各タブを scanned / unknown(blocked, excluded, injectFailed, timeout(4秒), discarded, deferred) に分類し、`currentState.hits` として session に保存。GET_STATE で popup へ
- バッジ（Pro の件数表示 ON 時）: 件数 or `完全+表記違い` / 走査 0 件なら赤 `0` / 走査できたタブなしなら灰 `?`
- popup: 内訳「完全 n / 表記違い m」、警告文、判定不能タブ一覧（タイトル + 理由。URL は出さない。<title> 無しは「（無題）」）
- 判断して決めたこと: `about:blank`・新しいタブ・拡張自身のページは本文がないので集計から除外（毎回「一部確認できず」が出るのを防ぐため）。それ以外の `chrome://` は「拡張が動作しないページ」として判定不能に入れる
- e2e-pro 60/60、Phase 1 回帰 22/22 ×2。README に正規化で無視する文字の一覧、docs/privacy と index に「光った＝正しいではない」注意を追記

### 2026-09-10 Phase 4 完了（ストア申請の素材）

- `store/description_ja.md` / `description_en.md`: キャッチ「二画面照合を、選択するだけで」。冒頭で想定ユーザー（事務・経理・受注処理・データ入力）、困りごと → 解決の順。断定表現なし。「光った＝正しい、ではない」「確認できなかったタブ」を本文に明記。無料 / Pro を分けて記載
- `store/permissions_justification.md`: 単一用途、tabs / scripting / storage / `<all_urls>` の理由（日英）、リモートコードなし、データ使用の申告表
- スクショ 3 枚（`tools/shoot-store.js` で実機撮影、1280×800）
  1. チャット風の受注メモ → 注文入力フォーム。氏名を選択して右の入力欄と見出しが光る
  2. 電話番号 → 顧客一覧 200 行。同じ人がハイフンあり（黄）/ なし（橙）で二重登録されている場面 + ポップアップ「完全 1 / 表記違い 1」
  3. 商品コードの転記ミス（0815 → 0851）で 0 件。ポップアップに赤の「他のタブに一致はありません」
  - ダミーデータは架空（サンプル商店、架空区）。実在企業名なし
- **4秒タイムアウトの検証**: 顧客一覧で 200 行 265ms / 2000 行 341ms / 5000 行 499ms / 10000 行 681ms（選択 → mark 表示まで、200ms のデバウンス込み）。判定不能にならず。4秒で十分な余裕
- `package.sh`: src/ の中身だけを ZIP（icon.svg・.map・OS ファイルは除外）。`--verify` で展開 → e2e 22/22 ×2、60/60 を確認済み
- 未着手（任意）: プロモタイル 440×280、docs トップの LP 化（Phase 4 の素材ができたので着手できる）

## Phase ごとの残り

- [x] Phase 1 ストア公開の必須要件
- [x] Phase 2 プライバシーポリシーと README
- [x] Phase 3 Pro 機能とライセンス（実装・モック検証済み）
- [ ] Phase 3 補足: Polar sandbox で実機確認（organizationId とテストキーが必要）
- [x] Phase 4 ストア申請の素材
- [ ] 公開前: `license.js` に本番の organizationId / purchaseUrl を入れる、ストアに ZIP・スクショ・説明文を登録
